#!/usr/bin/env bash
# Minimal fixture plugin — exercises the macrift loader in the test suite.
# No top-level side effects; just defines the menu function the manifest names.

sample_plugin_menu() { :; }
