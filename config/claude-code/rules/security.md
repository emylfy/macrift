# Security Rules

- Never hardcode secrets, tokens, passwords in code
- Always validate user input before using in shell commands
- Don't log sensitive data (passwords, keys, personal data)
- Don't use eval/Invoke-Expression with dynamic content
- Delete temporary files with sensitive data after use
