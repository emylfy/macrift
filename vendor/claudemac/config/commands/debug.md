---
description: Run the `debugger` agent for error analysis
---

Run the `debugger` agent for error analysis.

Pass it:
1. Full error text / stack trace
2. File and line where it crashes (if known)
3. What was happening before the error

The agent will find the root cause and suggest a fix.
